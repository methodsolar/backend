# Solar Quote Backend

Simple FastAPI backend that fetches solar panel layout and quote based on address and monthly kWh usage.

## How to run:

1. Install requirements:

    pip install -r requirements.txt

2. Run the server:

    uvicorn main:app --reload
